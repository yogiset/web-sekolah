spring:

  mail:
    host: sandbox.smtp.mailtrap.io
    port: 2525
    username: 5c1ac69db862f6
    password: ce26d9e1a6a03f
    properties:
      mail:
        smtp:
          starttls:
            enable: true
          auth: true


  datasource:
    url: jdbc:postgresql://localhost:5432/sekolah
    username: postgres
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8082